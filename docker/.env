# ====================================
# DevOps运维管理系统环境变量配置
# ====================================
# 修改端口后执行: docker-compose down && docker-compose up -d

# ----------------
# 服务端口配置
# ----------------
# 前端访问端口(浏览器访问地址)
WEB_PORT=8080
# API后端端口
API_PORT=8000

# Prometheus监控端口
PROMETHEUS_PORT=9090

# Pushgateway推送端口
PUSHGATEWAY_PORT=9091

# ----------------
# 外网访问地址配置
# ----------------
# 图片和文件访问地址(修改为实际的IP或域名)
# 格式: http://your-ip:WEB_PORT 或 http://your-domain.com
# 示例: http://192.168.1.100:8080 或 http://devops.example.com
IMAGE_HOST=http://localhost:8080

# ----------------
# 数据库配置
# ----------------
# MySQL数据库端口
MYSQL_PORT=3306
# MySQL root密码
MYSQL_ROOT_PASSWORD=devops@2025
# 数据库名称
MYSQL_DATABASE=devops
# 数据库用户
DB_USER=root

# 数据库主机(容器环境不要改)
DB_HOST=mysql

# 数据库端口(容器内部端口,不要改)
DB_PORT=3306
# ----------------
# Redis配置
# ----------------
# Redis缓存端口
REDIS_PORT=6379
# Redis密码
REDIS_PASSWORD=zhangfan@123
# Redis地址(容器环境不要改)
REDIS_ADDR=redis:6379
# ----------------
# 系统配置
# ----------------
# 时区配置
TZ=Asia/Shanghai
